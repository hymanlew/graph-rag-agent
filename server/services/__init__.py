"""
服务层模块集合

该包包含系统所有核心业务服务的实现，是业务逻辑的核心层。
服务层负责处理复杂的业务逻辑，协调数据访问，提供统一的功能接口给上层使用。

主要服务组件：
- agent_service: 代理管理服务，负责创建、管理和调用不同类型的代理
- chat_service: 聊天服务，处理用户对话和响应生成
- kg_service: 知识图谱服务，提供知识图谱相关的业务功能

设计特点：
- 采用分层架构，将业务逻辑与API层分离
- 每个服务专注于特定领域的功能实现
- 提供标准化的接口，便于上层调用
- 支持服务间的协作和集成
"""